# quorum-raft-helm-template`

To deploy to your local kubernetes cluster run: 
```
helm install 3nodes quorum
```
run following command to get the networks entrypoint 
````
minikube service <quorumnode-entrypoint> --url
````

 